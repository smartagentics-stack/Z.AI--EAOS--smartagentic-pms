/**
 * @smartagentics/worker — Background worker application
 *
 * Phase E will add Restate workflow handlers.
 * For now, this app reserves the namespace.
 */
export const APP_NAME = '@smartagentics/worker' as const;
export const APP_VERSION = '0.1.0' as const;

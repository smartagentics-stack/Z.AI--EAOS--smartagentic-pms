{
  "name": "@smartagentics/engineering-assurance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Engineering Assurance Engine (EAE) — converts governance into executable verification",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "eae": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@smartagentics/dev-config": "workspace:*",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
